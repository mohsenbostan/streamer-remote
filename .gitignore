# build output
*.exe
*.exe.new
*.exe.old

# runtime state, never commit secrets or machine-local files
config.yaml
twitch_token.json
logs/

# frontend build artifacts (regenerated by `npm run build` in web/, which
# CI always runs before building the Go binary — see .github/workflows)
web/node_modules/
internal/webui/dist/
